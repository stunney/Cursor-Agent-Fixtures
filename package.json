{
  "name": "cursor-agent-fixtures",
  "version": "0.1.0",
  "description": "Deterministic agent workflow fixtures for Cursor (setup/teardown at each lifecycle stage)",
  "type": "module",
  "main": "dist/hook.js",
  "scripts": {
    "build": "tsc && npm run bundle",
    "bundle": "node build/bundle.mjs",
    "test": "npm run build && node --test dist/tests/**/*.test.js",
    "prepare": "npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2"
  },
  "license": "MIT"
}
